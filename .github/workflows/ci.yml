name: CI

on:
  push:
    branches: [master]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install package and test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install setuptools
          pip install -e .
          pip install pytest pytest-cov coverage "genbadge[coverage]"

      - name: Run tests with coverage
        run: |
          pytest --cov=gravilens --cov-report=term-missing --cov-report=xml --cov-report=html

      - name: Upload coverage HTML
        uses: actions/upload-artifact@v4
        with:
          name: coverage-html
          path: htmlcov

      - name: Upload coverage XML
        uses: actions/upload-artifact@v4
        with:
          name: coverage-xml
          path: coverage.xml

      - name: Generate coverage badge
        if: github.ref == 'refs/heads/master'
        run: |
          mkdir -p .github/badges
          python -m genbadge.main coverage -i coverage.xml -o .github/badges/coverage.svg

      - name: Commit coverage badge
        if: github.ref == 'refs/heads/master'
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Update coverage badge"
          file_pattern: .github/badges/coverage.svg